spring:
  datasource:
    mysql:
      url: jdbc:mysql://localhost:13306/ai_content?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
      username: ai_content
      password: ai_content123
      driver-class-name: com.mysql.cj.jdbc.Driver

    vector:
      url: jdbc:postgresql://localhost:5432/ai_rag_knowledge
      username: postgres
      password: postgres
      driver-class-name: org.postgresql.Driver
      hikari:
        minimum-idle: 5
        maximum-pool-size: 20
        idle-timeout: 30000
        connection-timeout: 30000

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  data:
    redis:
      host: localhost
      port: 16379
      password:
      database: 0


ollama:
  base-url: http://localhost:11434
  model: qwen3-embedding:0.6b

model:
  api-key-secret: local-dev-secret

logging:
  level:
    com.ai.content: DEBUG
    org.hibernate.SQL: DEBUG
